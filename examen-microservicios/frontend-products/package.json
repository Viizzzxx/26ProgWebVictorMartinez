{
  "name": "frontend-products",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/main.ts --outfile=./dist/main.js",
    "dev": "bun run build && bunx serve . -l tcp://0.0.0.0:5172",
    "start": "bun run build && bunx serve . -l tcp://0.0.0.0:5172"
  },
  "devDependencies": {}
}